[package]
name = "gooz-audio"
description = "Real-time audio engine v0: device I/O behind a backend seam, lock-free record/playback rings, takes"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
cpal = "0.15"
ringbuf = "0.4"
