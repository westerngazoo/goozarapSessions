{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs the Easy Mode studio shell needs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
